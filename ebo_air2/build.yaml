# Base image per architecture. python:3.11-slim is Debian/glibc: required by the Agora
# SDK (its .so files are glibc, not musl/alpine).
build_from:
  amd64: python:3.11-slim
labels:
  org.opencontainers.image.title: "EBO Air 2"
  org.opencontainers.image.source: "https://github.com/Playcolors-co/ha-ebo-air2"
